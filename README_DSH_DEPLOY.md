# Deploy note

Hướng dẫn đầy đủ (kiến trúc, máy build, máy khác, Browser Use, checklist): **[BUILD.md](BUILD.md)**. Tổng quan: **[README.md](README.md)**.

File này không còn là bản compose mẫu. Compose thật: `docker-compose.yml` (DSH không publish port; `web-gateway` bind `127.0.0.1:3080`).
